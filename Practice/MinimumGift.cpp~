#include<bits/stdc++.h>
using namespace std;


//Not working soluction 
//This que asked in tcs codevita Aug 2020
void findMinimumGiftOfEmployess(int _rank[],int nnn,int xxx) {

  int sum[xxx],token2;
  
  for(token2=0; token2 <xxx; token2++) {
    
      sum[token2]=1;
  }
  
  cout<<"Display: "<<endl;
  
  for(int i=0; i<<xxx; i++) {
    cout<<sum[i]<<" "<<endl;
  }
  

 
}

int main() {
  
  int _testCases;
  cin>>_testCases;
  while(_testCases--) {
    int _noOfEmployees;
    cin>>_noOfEmployees;
    int _rankOfEmp[_noOfEmployees];
    
    //Accept the rank of employees
    for(int token1=0; token1<_noOfEmployees; token1++) {
      cin>>_rankOfEmp[token1];
    }
    
    int n = sizeof(_rankOfEmp)/sizeof(_rankOfEmp[0]);
    
    findMinimumGiftOfEmployess(_rankOfEmp, n,_noOfEmployees);
   }   
    
  return 0;
}
