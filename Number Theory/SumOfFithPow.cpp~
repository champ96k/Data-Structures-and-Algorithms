#include <bits/stdc++.h>
using namespace std;

void sumOfFifth(int n) {
  long long int i,sum=0,n;
  for(i=1; i<=n;i++) {
    sum=sum+(i*i*i*i*i);
  }
  cout<<sum<<endl;
}

int main() {
	//code
	int t;
	cin>>t;
	while(t--) {
	  int num;
	  cin>>num;
	 sumOfFifth(num);
	}
	return 0; 
}
