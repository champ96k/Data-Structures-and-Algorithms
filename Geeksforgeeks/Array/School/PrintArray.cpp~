//Tushar Nikam codes
#include<bits/stdc++.h>
using namespace std;

int printIndex(int a[],int x) {
    
    return a[x];
}
int main()
{
    //T for Number of Test Cases
    int T;
    cin>>T;
    while(T--)
    {
        //start here
        int n;
        cin>>n;
        int a[n];
        int x;
        cin>>x;
        
        for(int i=0;i<n;i++) {
            cin>>a[i];
        }
        cout<<printIndex(a,x)<<endl;
    }
    return 0;
}
