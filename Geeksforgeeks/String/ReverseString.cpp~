#include<bits/stdc++.h>
using namespace std;

void reverseArry(string str) {

  for(int j=str.length()-1;j>=0;j--) {
    cout<<str[j];
  }
  
}

int main() {
  long long t;
  cin>>t;
  while(t--) {
    
    string str;
    cin>>str;
   reverseArry(str);
   cout<<endl;
  
  }
}
